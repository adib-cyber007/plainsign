{
  "name": "plainsign-demo-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "chain": "hardhat node",
    "test": "hardhat test",
    "typecheck": "tsc --noEmit",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "seed:local": "hardhat run scripts/fund-and-mint.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@types/node": "^22.7.4",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "solc": "0.8.24",
    "typescript": "^5.5.0"
  }
}
